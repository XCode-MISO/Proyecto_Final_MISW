name: unit-ms_compras

on:
  pull_request:
    paths:
      - "services/ms_compras/**"
  push:
    branches: [master]
    paths:
      - "services/ms_compras/**"

jobs:
  unit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r services/ms_compras/requirements.txt
          pip install pytest pytest-cov matplotlib lxml

      - name: Run tests & generate coverage reports
        working-directory: services/ms_compras
        run: |
          pytest -q tests \
            --cov=. \
            --cov-report=xml:coverage.xml \
            --cov-report=html:htmlcov

      - name: Generate coverage chart
        working-directory: services/ms_compras
        run: |
          python scripts/generate_coverage_chart.py \
            coverage.xml \
            coverage_chart.png

      - name: Embed coverage chart in summary
        # Esto añade la imagen al resumen del paso, convertida a Base64
        run: |
          echo "### 📊 Coverage" >> $GITHUB_STEP_SUMMARY
          echo "![coverage](data:image/png;base64,$(base64 -w0 coverage_chart.png))" \
            >> $GITHUB_STEP_SUMMARY

      - name: Upload coverage artifacts
        uses: actions/upload-artifact@v4
        with:
          name: coverage-ms_compras
          path: |
            services/ms_compras/htmlcov
            services/ms_compras/coverage_chart.png