name: unit-recomendations-commnad

on:
  pull_request:
    paths:
      - "services/recomendations-commnad/**"
      - ".github/workflows/unit-recomendations-commnad.yml"
  push:
    branches: [master]
    paths:
      - "services/recomendations-commnad/**"

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.9"}

      - name: Install deps
        run: |
          pip install -r services/recomendations-commnad/requirements.txt
          pip install pytest pytest-cov

      - name: Run tests + coverage
        run: |
          pytest -q services/recomendations-commnad/tests \
            --cov=services/recomendations-commnad \
            --cov-report=xml \
            --cov-report=html:cov_html

      - uses: actions/upload-artifact@v4
        with:
          name: coverage-recomendations-commnad
          path: cov_html